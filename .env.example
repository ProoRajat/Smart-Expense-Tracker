# Copy to .env and fill in. Used by the frontend (Vite).

# Backend API URL — set this to use real auth and persistence (e.g. http://localhost:3001)
# If unset, the app runs with mock data only.
# VITE_API_URL=http://localhost:3001
